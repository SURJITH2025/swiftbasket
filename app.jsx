import React from 'react'
import ReactDOM from 'react-dom/client'
import SwiftBasket from './SwiftBasket_v2'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SwiftBasket />
  </React.StrictMode>
)
